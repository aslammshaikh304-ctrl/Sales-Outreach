import { NextResponse } from "next/server"; import { isAuthorized, unauthorized } from "@/lib/server/auth"; import { parseLeadInput } from "@/lib/server/lead-validation"; import { postWebhook, webhookError } from "@/lib/server/webhook";
export async function POST(request: Request) { if (!isAuthorized(request)) return unauthorized(); let body: unknown; try { body = await request.json(); } catch { return NextResponse.json({ success: false, error: "Valid JSON is required." }, { status: 400 }); } const parsed = parseLeadInput(body); if (!parsed.value) return NextResponse.json({ success: false, error: parsed.error }, { status: 400 }); try { const response = await postWebhook("N8N_ADD_LEAD_WEBHOOK_URL", parsed.value); if (!response.ok) return NextResponse.json({ success: false, error: "Add lead service failed." }, { status: 502 }); return NextResponse.json({ success: true }); } catch (caught) { const failure = webhookError(caught); return NextResponse.json({ success: false, error: failure.message }, { status: failure.status }); } }
